"""SFS format version — independent of package version.

Only bump when the .sfs specification changes (new required fields,
schema changes, breaking format changes). Do NOT bump on feature releases.
"""

SFS_FORMAT_VERSION = "0.1.0"
SFS_CONVERTER_VERSION = "0.1.0"
